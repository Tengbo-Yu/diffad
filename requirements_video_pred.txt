# Requirements for Video Prediction Task
# Install with: pip install -r requirements_video_pred.txt

# Core dependencies
torch
torchvision
diffusers>=0.21.0
einops>=0.7.0
pyyaml>=6.0
tqdm>=4.65.0
pillow>=10.0.0
numpy>=1.24.0

# Training and logging
tensorboard>=2.13.0
tensorboardX>=2.6

# Optional but recommended
# For better image quality metrics
# lpips>=0.1.4
# pytorch-fid>=0.3.0

# For distributed training (usually included with PyTorch)
# accelerate>=0.20.0

